type Readings @model @auth(rules: [{allow: public}, {allow: private, operations: [update], provider: iam}]) {
  id: ID!
  humidity: Float!
  timestamp: AWSTimestamp!
  device: String!
  User: AWSEmail
}
 